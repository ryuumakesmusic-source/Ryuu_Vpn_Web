{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "check-expiring-plans": "tsx ./check-expiring-plans.ts",
    "build": "esbuild check-expiring-plans.ts --bundle --platform=node --format=esm --outfile=dist/check-expiring-plans.js --external:pg-native",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "drizzle-orm": "catalog:",
    "esbuild": "catalog:",
    "tsx": "catalog:"
  }
}
